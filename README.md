# PulseBoard

Учебная панель мониторинга, объединяющая три публичных источника данных:

- Open-Meteo API: текущая погода в Москве.
- CoinGecko API: цены Bitcoin и Ethereum и изменение за 24 часа.
- GitHub REST API: показатели репозитория `vercel/next.js`.

Проект является статическим сайтом: запросы отправляются из браузера, API-ключи
и серверная конфигурация не нужны.

## Опубликованная версия

https://monitoring-livid.vercel.app

## Локальный запуск

```bash
npx serve .
```

Или откройте `index.html` через расширение Live Server.

## Публикация на Vercel

```bash
npx vercel --prod
```

При первом запуске Vercel попросит войти в аккаунт и подтвердить настройки
проекта. В качестве корневой директории используется текущая папка, команда
сборки не требуется.

## Отправка задания

В поле «Ваш ответ» вставьте адрес
`https://monitoring-livid.vercel.app`. В поле загрузки файла можно приложить,
например, `index.html` из этого проекта: его размер значительно меньше 200 МБ.
